version: '3'
services:
  redis:
    image: redis:6-alpine
    ports:
      - 6379:6379
    restart: always
  flask:
    #현재 위치에 있는 Dockerfile을 빌드에 사요한다.
    build: .
    ports:
      - 9000:9000
    depends_on:
      - redis
    restart: always
